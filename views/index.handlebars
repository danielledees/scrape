
<div class="container">
    <div class="col-md-12 jumbotron">
        <div class="text-center">
            <h1> Resident Advisor Music News Mongo Scraper</h1>
        </div>

    </div>
    <div class="row">
        <div class="col-md-12">
            {{#if article}}
                {{#each article}}
                <div class="card">
                    <h5 class="card-header">{{this.title}}</h5>
                    <div class="card-body">
                        <h5 class="card-title">{{this.headline}}</h5>
                        <a href="https://www.residentadvisor.net/{{this.link}}"" target="_blank">"https://www.residentadvisor.net/{{this.link}}</a>
                        <br>
                        <br>
                        <button type="button" class="btn btn-success" id="save" data-id={{this._id}}>Save Article</button>
                    </div>
                    </div>
                {{/each}}
            {{else}}
                <div class="card">
                    <h5 class="card-header"></h5>
                    <div class="card-body">
                        <h5 class="card-title">Uh Oh. Looks like we don't have any new articles</h5>
                        <a href="/scrape" class="btn btn-primary scrape">Scrape New Articles</a>
                    </div>
                    </div>
            {{/if}}
        </div>
    </div>
</div>